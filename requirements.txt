# requirements.txt — Cross-Chain Bridge Tracer (bridge_tracer.py и весь пакет
# aml/, от которого он зависит: tron_adapter, evm_adapter, common).
#
# pip install -r requirements.txt --break-system-packages

# --- Сетевые клиенты ---
requests           # layerzero_tracer.py, usdt0_deployments.py (синхронный клиент к LayerZero Scan / USDT0 Deployments API)
aiohttp             # aml/evm_adapter, aml/tron_adapter (асинхронные клиенты к Blockscout Pro / TronGrid)

# --- Кэш и конфигурация ---
aiosqlite           # aml/evm_adapter/cache.py, aml/tron_adapter/cache.py (диск-кэш ответов)
pyyaml              # aml/evm_adapter/config/chains.yaml, aml/flow_tracer/config/*.yaml
python-dotenv       # aml/common/secrets.py — автозагрузка .env

# --- Только для полного набора тестов (aml/, не используется bridge_tracer.py напрямую) ---
asyncpg             # aml/common/db.py — импортируется транзитивно через common.evidence_store
                    # (flow_tracer/attribution модули), даже если сама БД не используется в тестах трейсера
ollama              # aml/agent/orchestrator.py — импортируется тестами aml/agent/tests/ (типы сообщений),
                    # реальный Ollama-сервер для тестов не нужен

# --- Тестирование ---
pytest
pytest-asyncio
